<?xml version="1.0" encoding="utf-8"?>

<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="com.opp.android.exchange.CountryViewModel"/>
    </data>
    <android.support.v7.widget.CardView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        app:cardCornerRadius="5dp"
        app:elevation="5dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#ffff"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/list_item_flag_image_view"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_gravity="center_vertical"
                android:layout_margin="4dp"
                app:flagpng="@{viewModel.flag}"/>

            <TextView
                android:id="@+id/list_item_country_chinese_text_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:paddingLeft="4dp"
                android:textStyle="bold"
                android:text="@{viewModel.name}"/>

            <TextView
                android:id="@+id/list_item_country_english_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:textStyle="bold"
                android:gravity="right"
                android:paddingRight="15dp"
                android:text="@{viewModel.currency}"
                tools:text="Country currency"/>

        </LinearLayout>

    </android.support.v7.widget.CardView>
</layout>